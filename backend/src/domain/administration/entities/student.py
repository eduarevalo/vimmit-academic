from domain.shared.baseEntity import BaseEntity


class Student(BaseEntity):
    """
    Student Entity.
    No core fields defined yet.
    Tenant-specific fields are stored in the inherited dynamicAttributes.
    """

    pass
